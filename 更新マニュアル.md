# ReAct!! ホームページ 更新マニュアル

プログラミングの知識がなくても大丈夫です。このマニュアルの通りに操作すれば更新できます。

サイトの更新は、GitHubというサービスの「ファイルを直接ブラウザで編集する機能」を使います。
（アプリのインストールは不要です）

---

## 1. 活動報告に新しいInstagram投稿を追加する

### 手順

1. **Instagramで紹介したい投稿を開く**（パソコンのブラウザで開いてください）
2. 投稿右上の「**…**」（もっと見るメニュー）をクリック
3. 「**埋め込み**」を選択
4. 「**埋め込みコードをコピー**」をクリック（`<blockquote ...>` から始まるコードがコピーされます）
5. GitHubでこのサイトのリポジトリを開き、`site/index.html` ファイルを開く
6. 右上の鉛筆マーク（✏️ Edit this file）をクリックして編集モードにする
7. ページ内を **Ctrl+F**（Macは Cmd+F）で検索し、
   `新しい投稿はこの行の下にコピペしてください` という文字を探す
8. その行のすぐ下に、以下のように貼り付ける

   ```html
   <div class="activity-post">
     （ここに手順4でコピーした<blockquote>〜</blockquote>のコードを貼り付ける）
   </div>
   ```

   ※ 既にある投稿ブロック（`<div class="activity-post"> ... </div>`）をそのままコピーして、
   `data-instgrm-permalink="..."` の部分だけ新しい投稿のURLに書き換える方法でも構いません。

9. 画面下までスクロールし、「**Commit changes...**」ボタンを押して保存する
10. 数分待ってからサイトを開き、反映されているか確認する

### 古い投稿を減らしたいとき
同じように `site/index.html` を開き、削除したい `<div class="activity-post"> ... </div>` の
ブロックをまるごと選択して削除し、保存してください。

---

## 2. メンバー募集フォームのリンクを設定する

新しい常設の申し込みフォーム（Googleフォームなど）ができたら、以下の手順でリンクを差し替えます。

1. `site/index.html` を開く
2. `参加申し込みフォーム（準備中）` という文字を検索する
3. その手前にある `href="#"` の `#` の部分を、フォームのURL（例：`https://forms.gle/xxxxxxx`）に書き換える

   変更前：
   ```html
   <a href="#" class="btn btn-light" id="join-form-link">参加申し込みフォーム（準備中）</a>
   ```
   変更後（例）：
   ```html
   <a href="https://forms.gle/xxxxxxx" class="btn btn-light" id="join-form-link">参加申し込みフォーム</a>
   ```
   ※ ボタンの文字から「（準備中）」も削除しておくと良いです。

4. 保存（Commit changes...）する

---

## 3. 困ったときは

- 誤って何かを消してしまって元に戻したい場合は、GitHubの「History（変更履歴）」から
  以前の状態に戻すことができます。不安な場合は保存前にご相談ください。
- 上記以外の大きな変更（デザインの変更、ページの追加など）は、開発を依頼した担当者に
  ご相談ください。
