# ReAct!! ホームページ 更新マニュアル

プログラミングの知識がなくても大丈夫です。このマニュアルの通りに操作すれば更新できます。

サイトの更新は、GitHubというサービスの「ファイルを直接ブラウザで編集する機能」を使います。
（アプリのインストールは不要です）

---

## 1. 活動報告（ブログ）に新しい記事を追加する

活動報告はブログ形式になっていて、`site/js/posts-data.js` というファイルに
記事を1つ追記するだけで、サイトに自動で反映されます。HTMLの知識は不要です。

### 手順

1. GitHubでこのサイトのリポジトリを開き、`site/js/posts-data.js` ファイルを開く
2. 右上の鉛筆マーク（✏️ Edit this file）をクリックして編集モードにする
3. ファイルの一番上、`const REACT_POSTS = [` のすぐ下に、以下の形式で1つ追加する

   ```js
   {
     date: "2026-09-10",
     title: "〇〇祭りに出演しました！",
     body: "本日は〇〇祭りに出演させていただきました。\n応援してくださった皆様、本当にありがとうございました！"
   },
   ```

   - `date` は「年-月-日」の形式（新しい記事ほど上に表示されるよう自動で並び替わるので、
     どこに追記しても構いません）
   - `title` は記事のタイトル
   - `body` は本文。改行したい場所には `\n` を入れる
   - 各項目の最後の **カンマ（,）を忘れずに**つける（次の `{` との区切りのため）

4. 画面下までスクロールし、「**Commit changes...**」ボタンを押して保存する
5. 数分待ってからサイトを開き、反映されているか確認する

### 記事を削除・修正したいとき
同じファイルの中から、該当する `{ date: ..., title: ..., body: ... },` のかたまりを
探して、書き換える（修正の場合）か、まるごと削除する（削除の場合）。

### 表示について（知っておくと安心なこと）
- トップページの活動報告には、**最新3件**だけが表示されます（それ以上はサイトが長くなりすぎないように）
- 4件目以降の記事は、自動的に「活動報告の一覧を見る（Read more）」ボタンから見られる
  一覧ページ（`activities.html`）にまとめて表示されます
- 一覧ページでは、年ごとの絞り込みボタンとキーワード検索が自動でついています
  （`posts-data.js` に記事を追加するだけで、これらも自動的に更新されます。追加の作業は不要です）

### Instagramの投稿を紹介したい場合
本文（`body`）の中に、投稿のURLをそのまま文章として書けば、リンクとして機能はしませんが
文字としては表示されます。リンクとして貼りたい場合は、担当者にご相談ください。

---

## 2. メンバー募集フォームのリンクを設定する

新しい常設の申し込みフォーム（Googleフォームなど）ができたら、以下の手順でリンクを差し替えます。

1. `site/index.html` を開く
2. `参加申し込みフォーム（準備中）` という文字を検索する
3. その手前にある `href="#"` の `#` の部分を、フォームのURL（例：`https://forms.gle/xxxxxxx`）に書き換える

   変更前：
   ```html
   <a href="#" class="btn btn-light" id="join-form-link">参加申し込みフォーム（準備中）</a>
   ```
   変更後（例）：
   ```html
   <a href="https://forms.gle/xxxxxxx" class="btn btn-light" id="join-form-link">参加申し込みフォーム</a>
   ```
   ※ ボタンの文字から「（準備中）」も削除しておくと良いです。

4. 保存（Commit changes...）する

---

## 3. 演舞依頼フォームを設定する

演舞依頼・お問い合わせセクションに、Googleフォームを埋め込むことができます。

### 手順

1. Googleフォームで、演舞依頼を受け付けるフォームを作成する
   （項目例：依頼者名、団体名、連絡先、希望日時、場所、依頼内容、予算感、**備考欄** など）
2. フォームの右上にある「**送信**」ボタンをクリック
3. 送信方法の選択肢の中から「**\<\>**」（埋め込み）のタブを選ぶ
4. 表示されたコード（`<iframe src="..." width="640" height="..." ...>...</iframe>` という
   形のコード）をコピーする
5. GitHubで `site/index.html` を開き、鉛筆マーク（✏️ Edit this file）で編集モードにする
6. **Ctrl+F**（Macは Cmd+F）で `ここにGoogleフォームの埋め込みiframeを貼り付け` を検索する
7. その行（コメント）を、手順4でコピーしたiframeのコードに置き換える
8. 「**Commit changes...**」ボタンを押して保存する

### 注意点
- Googleフォームの埋め込みiframeは、そのままだと縦に長くなりがちです。表示が変な場合は
  `height="900"` の数字部分を調整すると高さを変えられます。
- フォームの作成・編集自体はGoogleアカウントでの操作になります。

## 4. 困ったときは

- 誤って何かを消してしまって元に戻したい場合は、GitHubの「History（変更履歴）」から
  以前の状態に戻すことができます。不安な場合は保存前にご相談ください。
- 上記以外の大きな変更（デザインの変更、ページの追加など）は、開発を依頼した担当者に
  ご相談ください。
